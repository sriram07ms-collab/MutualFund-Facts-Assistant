name: Deploy Backend to Vercel

on:
  push:
    branches:
      - main
    paths:
      - 'api/**'
      - 'rag_pipeline.py'
      - 'vector_store.py'
      - 'data_collector.py'
      - 'config.py'
      - 'requirements.txt'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Backend API to Vercel
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  
    - name: Checkout code
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    
    - name: Install Vercel CLI
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: npm install -g vercel@latest
    
    - name: Pull Vercel Environment Information
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: vercel pull --yes --environment=production --token="${{ env.VERCEL_TOKEN }}"
    
    - name: Build Project Artifacts
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: vercel build --prod --token="${{ env.VERCEL_TOKEN }}"
    
    - name: Deploy Project Artifacts to Vercel
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: vercel deploy --prebuilt --prod --token="${{ env.VERCEL_TOKEN }}"
    
    - name: Get Deployment URL
      id: vercel
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: |
        DEPLOYMENT_URL=$(vercel ls --token="${{ env.VERCEL_TOKEN }}" | grep -oP 'https://[^\s]+' | head -1)
        echo "url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
    
    - name: Update Frontend with API URL
      if: ${{ steps.secrets_check.outputs.missing != 'true' }}
      run: |
        echo "✅ Backend deployed successfully!"
        echo "API URL: ${{ steps.vercel.outputs.url }}/api/query"
        echo "Update docs/index.html with this URL"

